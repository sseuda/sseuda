#server config
server:
  port: 8080
  address: 0.0.0.0

# jwt 토큰
jwt:
  secret: 3CRveG45XAO7ij5koXwir62gBWB8rpDWmBsZ6P+N5X+mehGmytVXQ4/Ie77+ME8T9ENh2pVeYv0AJZ3Cdkfqmw==

spring:
  #DB config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sseudadb?allowMultiQueries=true
    username: sseuda
    password: sseuda
  jpa:
    hibernate:
      ddl-auto: update

  #fileupload config
  servlet:
    multipart:
      enabled: true
      max-request-size: 10MB
      max-file-size: 10MB
  # MVC config
  mvc:
    static-path-pattern: /static/**

  web:
    resources:
      static-locations: classpath:/sseudaimgs/

# mail(google)
  mail:
    host: smtp.gmail.com
    port: 587
    username: jieune120@gmail.com
    password: boju vkii bdeb wdel
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

#Mybatis framework config
mybatis:
  mapper-locations: classpath:mapper/*.xml

image:
  # Local Dev
  add-resource-locations: classpath:/static/sseudaimgs/
  add-resource-handler: /sseudaimgs/**
  image-dir: src/main/resources/static/sseudaimgs/
  image-url: http://localhost:8080/sseudaimgs/

#springdoc open-api config
springdoc:
  swagger-ui:
    path: /swagger-ui.html

# logging config
logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace